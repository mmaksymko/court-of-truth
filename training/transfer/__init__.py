"""Transfer-evaluation package.

Split by responsibility: config, models, splits, dedup, thresholds, metrics,
experiments, cli. Import the symbols you need from their module directly, e.g.
``from training.transfer.experiments import run_experiment``. Run the baseline
experiment with ``python -m training.transfer``.
"""
